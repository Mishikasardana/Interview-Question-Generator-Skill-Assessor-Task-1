# Interview Intelligence Platform — dependencies

# Core
pydantic>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
pytest>=8.0.0

# Resume processing (PDF + DOCX)
pypdf>=4.0.0
python-docx>=1.1.0

# API layer
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Frontend
streamlit>=1.42.0
pandas>=2.0.0

# Google Sign-In (Streamlit's native st.login()/st.user OIDC support)
Authlib>=1.3.2

# Speech-to-text (voice interview answers)
SpeechRecognition>=3.10.0

# Database (PostgreSQL persistence)
sqlalchemy>=2.0.0
psycopg[binary]>=3.1.0
bcrypt>=4.1.0

# Recruiter Intelligence Engine — skill ontology data file
PyYAML>=6.0.0
